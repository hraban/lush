language: go

go: 1.3

before_install:
- sudo apt-get update
- sudo apt-get install npm
- npm install -g gulp
- npm install -D gulp gulp-typescript

install: go get -d -v ./... && go build -v ./... && gulp

script: npm test
